\section{4 --- Two Group~2 v8 attempts}

\begin{frame}{Two v8 attempts in Group~2}
    \begin{columns}[T]
        \begin{column}{0.48\textwidth}
            \begin{block}{Fernanda's v8 (\texttt{v8} branch)}
                \footnotesize
                Repo: \texttt{UsingMatrixClassFork},
                branch \texttt{v8}
                (\texttt{CustomMemoryAllocator\_v8}).

                \vspace{0.3em}
                \textbf{Direction:} improve v7's
                \textbf{thread-cache} path ---
                still cache-first for small objects.

                \vspace{0.3em}
                Higher cache cap, live-block
                accounting, periodic retune,
                occupied-bin bitmaps, optional
                metrics + build scripts.
            \end{block}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{AI-generated v8 (ours)}
                \footnotesize
                Built/profiled for this report
                (M3 Max matrix campaign).

                \vspace{0.3em}
                \textbf{Direction:} matrix
                \textbf{row packing} --- turn the
                hot-path cache \emph{off}.

                \vspace{0.3em}
                Split arena, 64-align medium rows,
                page-align @ 2\,MiB, exact
                remainders, \texttt{pthread} +
                recent-large free.
            \end{block}
        \end{column}
    \end{columns}

    \vspace{0.35em}
    {\footnotesize Results slides use the \textbf{AI-generated} v8 unless noted.}
\end{frame}

\begin{frame}{What Fernanda's v8 changed (from v7)}
    {\scriptsize
    \renewcommand{\arraystretch}{1.35}
    \begin{tabular}{@{}p{0.34\textwidth}p{0.58\textwidth}@{}}
        \toprule
        Change & Effect \\
        \midrule
        Max cache / class \(256\rightarrow512\) &
          Larger per-class LIFO retention \\
        Live-block \texttt{CacheAccounting} &
          Track how many blocks of each class are live \\
        Retune every 256 allocs &
          Raise/lower targets from peak live use
          (cushion 4; min 8) \\
        Occupied-bin bitmaps &
          Skip empty size classes in search \\
        Optional metrics build &
          Hits/misses, refills, flushes, mutex takes,
          coalesce events \\
        Scripts (\texttt{v8-builds.sh}, \ldots) &
          Build metrics + Release; run workload pairs \\
        \bottomrule
    \end{tabular}
    }

    \vspace{0.4em}
    {\footnotesize Still like v7 on the big policy: \textbf{hot path uses
    \texttt{takeCachedBlock}}; \texttt{std::mutex}; page-align @ 64\,KiB.}
\end{frame}

\begin{frame}{Side-by-side: Fernanda v8 vs AI v8}
    {\scriptsize
    \renewcommand{\arraystretch}{1.4}
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{@{}p{0.20\textwidth}p{0.36\textwidth}p{0.36\textwidth}@{}}
        \toprule
        & \textbf{Fernanda v8} & \textbf{AI-generated v8} \\
        \midrule
        Goal &
          Smarter tiny-object cache &
          Packed matrix rows \\
        Hot path &
          Thread cache (kept) &
          Cache skipped \\
        Arena &
          One mmap region &
          Small + guard + large \\
        Cache cap &
          Up to 512 / class &
          Not used on hot path \\
        Align &
          Page @ 64\,KiB &
          64-byte @ \(\ge\)2\,KiB; page @ 2\,MiB \\
        Locking &
          \texttt{std::mutex} &
          \texttt{pthread} + recent-large \\
        Best for &
          Many small allocs (list-ish) &
          Reused medium rows (matrix) \\
        \bottomrule
    \end{tabular}
    }
\end{frame}

\begin{frame}{What the AI v8 changed (and why lists break)}
    {\scriptsize
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{@{}p{0.30\textwidth}p{0.62\textwidth}@{}}
        \toprule
        Change & Why \\
        \midrule
        Skip thread cache & LIFO cache scatters packed rows \\
        Split arena + guard & Tiny frees cannot punch row region \\
        64-byte align \(\ge\)2\,KiB & Double rows without page waste \\
        Page-align only @ 2\,MiB & 64\,KiB page-align wastes KiB rows \\
        Exact remainders & Packed stride between rows \\
        \bottomrule
    \end{tabular}
    }

    \vspace{0.35em}
    \textbf{Lists break:} millions of tiny nodes + cache off \(\Rightarrow\)
    locked best-fit every time; 16\,MiB small arena spills.

    \vspace{0.25em}
    {\footnotesize Pattern-optimized (profiled \(n{=}360\)); not
    \texttt{if (size==2880)}. Fernanda's cache path is the opposite bet.}
\end{frame}
